<%= fields= [ "First Name", "Email Address", "Favorite Food" ].map(&:to_s).join(",") %>
<% @users.map do |user| %>
    <% firstname =user.firstname %>
    <% email = user.email%>
    <% food = user.food%>
<%= [user.firstname, user.email, user.food].map(&:to_s).join(",") %>
<%end%>
